*n = bn_edge_height(t) + 1;
// Cấp phát bộ nhớ cho a
a = malloc(...)
sử dụng queue để duyệt các nút theo chiều rộng.
    dll với gtype
      push_back
      pop_front

Khởi tạo hàng đợi với t->root

* Lưu ý: Có thể xử lý 1 lượt tất cả các phần tử đang có
trong hàng đợi (tương ứng với 1 tầng) thay vì lấy ra
từng phần tử.

Lưu kết quả vào sll_t cho gtype

a[0] = sll_create_list();
...